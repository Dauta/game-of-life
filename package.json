{
  "name": "game-of-life",
  "version": "0.0.1",
  "description": "simple p5 implementation of Conway's game of life",
  "main": "index.js",
  "author": "github.com/Dauta",
  "license": "MIT",
  "scripts": {
    "build": "esbuild ./src --outfile=public/dist/index.js --bundle --minify",
    "watch": "esbuild --servedir=public --outfile=public/dist/index.js --bundle"
  },
  "devDependencies": {
    "@types/p5": "^1.3.0",
    "@types/randomcolor": "^0.5.6",
    "esbuild": "^0.12.15",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "p5": "^1.4.0"
  }
}
